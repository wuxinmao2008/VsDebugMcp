<platformUI:DialogWindow x:Class="VsDebugMcp_Vsix.ClientConfigWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
    xmlns:platformUI="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
    mc:Ignorable="d"
    Title="VsDebugMcp 客户端配置"
    Width="720" Height="540"
    MinWidth="620" MinHeight="460"
    WindowStartupLocation="CenterOwner"
    ResizeMode="CanResizeWithGrip"
    Background="{DynamicResource {x:Static vsshell:VsBrushes.WindowKey}}"
    Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}"
    FontFamily="Segoe UI, Microsoft YaHei, sans-serif"
    FontSize="12">

    <Window.Resources>
        <!-- 药丸式客户端单选按钮样式 -->
        <Style x:Key="SegmentRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="{DynamicResource {x:Static vsshell:VsBrushes.ButtonFaceKey}}" />
            <Setter Property="Foreground" Value="{DynamicResource {x:Static vsshell:VsBrushes.ButtonTextKey}}" />
            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static vsshell:VsBrushes.ComboBoxBorderKey}}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="11,5" />
            <Setter Property="Margin" Value="0,0,6,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource {x:Static vsshell:VsBrushes.HighlightKey}}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource {x:Static vsshell:VsBrushes.HighlightKey}}" />
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static vsshell:VsBrushes.HighlightTextKey}}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource {x:Static vsshell:VsBrushes.HighlightKey}}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- VS 主题标准按钮样式 -->
        <Style x:Key="VsButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource {x:Static vsshell:VsBrushes.ButtonFaceKey}}" />
            <Setter Property="Foreground" Value="{DynamicResource {x:Static vsshell:VsBrushes.ButtonTextKey}}" />
            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static vsshell:VsBrushes.ComboBoxBorderKey}}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,4" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource {x:Static vsshell:VsBrushes.HighlightKey}}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 强调操作主按钮样式 -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource VsButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource {x:Static vsshell:VsBrushes.HighlightKey}}" />
            <Setter Property="Foreground" Value="{DynamicResource {x:Static vsshell:VsBrushes.HighlightTextKey}}" />
            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static vsshell:VsBrushes.HighlightKey}}" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- 0. 顶部控制条：客户端切换 + 范围 + 状态 -->
            <RowDefinition Height="*" />    <!-- 1. 核心卡片：路径条 + 代码展示 + 复制 -->
            <RowDefinition Height="Auto" /> <!-- 2. 底部栏：说明 + 操作 -->
        </Grid.RowDefinitions>

        <!-- 0. 顶部控制条 -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- 客户端切换按钮组 -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="目标客户端:"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Margin="0,0,10,0"
                           Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}" />

                <RadioButton x:Name="RbVsCode" Content="VS Code" GroupName="ClientSelection"
                             Style="{StaticResource SegmentRadioButtonStyle}" IsChecked="True" Checked="OnClientSelectionChanged" />
                <RadioButton x:Name="RbCursor" Content="Cursor" GroupName="ClientSelection"
                             Style="{StaticResource SegmentRadioButtonStyle}" Checked="OnClientSelectionChanged" />
                <RadioButton x:Name="RbClaude" Content="Claude Desktop" GroupName="ClientSelection"
                             Style="{StaticResource SegmentRadioButtonStyle}" Checked="OnClientSelectionChanged" />
                <RadioButton x:Name="RbAntigravity" Content="Antigravity" GroupName="ClientSelection"
                             Style="{StaticResource SegmentRadioButtonStyle}" Checked="OnClientSelectionChanged" />
                <RadioButton x:Name="RbCodex" Content="Codex" GroupName="ClientSelection"
                             Style="{StaticResource SegmentRadioButtonStyle}" Checked="OnClientSelectionChanged" />
            </StackPanel>

            <!-- 右侧服务状态与端口 -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Border Background="#1A10B981" BorderBrush="#3310B981" BorderThickness="1" CornerRadius="3" Padding="6,3" Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="● " Foreground="#10B981" FontSize="10" VerticalAlignment="Center" />
                        <TextBlock x:Name="TxtStatus" Text="服务运行中" FontSize="11" Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}" VerticalAlignment="Center" />
                    </StackPanel>
                </Border>
                <TextBlock x:Name="TxtPort" Text="端口: 43260"
                           Foreground="{DynamicResource {x:Static vsshell:VsBrushes.GrayTextKey}}"
                           VerticalAlignment="Center" FontSize="11" />
            </StackPanel>
        </Grid>

        <!-- 1. 核心卡片容器 -->
        <Border Grid.Row="1"
                Background="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBackgroundKey}}"
                BorderBrush="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBorderKey}}"
                BorderThickness="1"
                CornerRadius="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" /> <!-- 路径与范围栏 -->
                    <RowDefinition Height="*" />    <!-- JSON 代码与浮动操作区 -->
                </Grid.RowDefinitions>

                <!-- 路径与范围栏 -->
                <Border Grid.Row="0"
                        Background="{DynamicResource {x:Static vsshell:VsBrushes.CommandBarHoverKey}}"
                        BorderBrush="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBorderKey}}"
                        BorderThickness="0,0,0,1"
                        Padding="10,6">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!-- 生效范围选择 -->
                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,12,0">
                            <RadioButton x:Name="RbScopeGlobal" Content="全局" GroupName="ScopeSelection"
                                         IsChecked="True" VerticalAlignment="Center" Margin="0,0,8,0"
                                         Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}"
                                         Checked="OnScopeChanged" />
                            <RadioButton x:Name="RbScopeLocal" Content="项目" GroupName="ScopeSelection"
                                         VerticalAlignment="Center"
                                         Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}"
                                         Checked="OnScopeChanged" />
                        </StackPanel>

                        <!-- 路径文本 -->
                        <Grid Grid.Column="1" VerticalAlignment="Center" Margin="0,0,10,0">
                            <TextBox x:Name="TxtPath"
                                     IsReadOnly="True"
                                     BorderThickness="0"
                                     Background="Transparent"
                                     Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}"
                                     FontFamily="Cascadia Mono, Consolas, Courier New"
                                     FontSize="11"
                                     VerticalAlignment="Center" />
                        </Grid>

                        <!-- 路径快捷按钮 -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button x:Name="BtnCopyPath" Content="复制路径"
                                    Style="{StaticResource VsButtonStyle}"
                                    Padding="8,3" FontSize="11" Margin="0,0,6,0"
                                    Click="OnCopyPathClick" />
                            <Button x:Name="BtnOpenFolder" Content="打开目录"
                                    Style="{StaticResource VsButtonStyle}"
                                    Padding="8,3" FontSize="11"
                                    Click="OnOpenFolderClick" />
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- JSON 代码框与右上角快速复制 -->
                <Grid Grid.Row="1">
                    <TextBox x:Name="TxtSample"
                             IsReadOnly="True"
                             AcceptsReturn="True"
                             AcceptsTab="True"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             Background="Transparent"
                             Foreground="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}"
                             BorderThickness="0"
                             FontFamily="Cascadia Mono, Consolas, Courier New"
                             FontSize="12"
                             Padding="12,10,12,10" />

                    <!-- 右上角悬浮复制按钮 -->
                    <Button x:Name="BtnCopyJson"
                            Content="复制配置"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Padding="14,6"
                            VerticalAlignment="Top"
                            HorizontalAlignment="Right"
                            Margin="0,10,12,0"
                            Click="OnCopyJsonClick" />
                </Grid>
            </Grid>
        </Border>

        <!-- 2. 底部辅助信息与操作 -->
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- 动态简述提示 -->
            <TextBlock x:Name="LblNotes"
                       Grid.Column="0"
                       VerticalAlignment="Center"
                       TextWrapping="Wrap"
                       FontSize="11"
                       Foreground="{DynamicResource {x:Static vsshell:VsBrushes.GrayTextKey}}"
                       Text="提示：将上述配置复制到对应客户端的配置文件中即可生效。" />

            <!-- 关闭按钮 -->
            <Button Grid.Column="1"
                    Content="关闭"
                    IsCancel="True"
                    Style="{StaticResource VsButtonStyle}"
                    Padding="16,5"
                    Click="OnCloseClick" />
        </Grid>
    </Grid>
</platformUI:DialogWindow>
